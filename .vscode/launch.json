{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Love2D Project",
            "type": "lua-local", // Or "node", "python", etc., depending on your debugger extension
            "request": "launch",
            "program": {
                "command": "love" // The command to execute the LÖVE2D engine
            },
            "args": [
                "." // This argument tells LÖVE2D to run the current directory as a game
            ],
            "cwd": "${workspaceFolder}" // Ensures the command runs from the project root
        }
    ]
}